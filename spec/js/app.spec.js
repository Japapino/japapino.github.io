describe('My Application', function() {
	describe('move to next picture', function() {
		it('increases the index on button push', function() {
			expect(nextSlide(1)).toEqual(2)
		});
	})
	//describe('toggle drop down menu', function() {
	// tests about `someOtherFunction()`
//})
})
